import React from 'react';

const CheckOut = () => {
  // const [details] = (props.location && props.location.burgerBuilder) || [{}];
  // console.log(details);
  return (
    <div>CheckOut</div>
  );
};

export default CheckOut;